# Pooled connection (for app)
DATABASE_URL="postgresql://user:password@your-endpoint-pooler.region.aws.neon.tech/dbname?sslmode=require&connect_timeout=15"
# Direct connection (for Prisma CLI: db push, migrate) — same URL but host without -pooler
DIRECT_URL="postgresql://user:password@your-endpoint.region.aws.neon.tech/dbname?sslmode=require&connect_timeout=15"
PORT=3001
